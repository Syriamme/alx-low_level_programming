#include <stdlib.h>
#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (success)
 */

int main(void)
{
	int abc;

	for (abc = 'a'; abc <= 'z'; abc++)
	{
		putchar(abc);
	}
		putchar('\n');
	return (0);
}
