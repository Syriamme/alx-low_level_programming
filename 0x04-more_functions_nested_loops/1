#include "main.h"

/**
*_isupper - a function that checks for uppercase characte*
*@c - input character
*Returns 1 if c is uppercase
*Returns 0 otherwise
*/

int _isupper(int c);
{
	if (x = 'A' && x <= 'Z')

	return (1);

	else
		return (0);
}
