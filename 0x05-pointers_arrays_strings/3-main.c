#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    char *str;

    str = "I do not fear Kevsam. I fear who Kevsam will be - Isaac Asimov";
    _puts(str);
    return (0);
}
